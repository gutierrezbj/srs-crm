# SpotterSRS Environment Configuration for Docker
# Copy this file to backend/.env or configure in docker-compose.yml

# Certificate paths (Docker environment)
CERT_DIR=/app/certs
PLACSP_CERT_PATH=/app/certs/client_cert.pem
PLACSP_KEY_PATH=/app/certs/client_key_nopass.pem

# Feed URL (personalized PLACSP feed requiring certificate auth)
PLACSP_FEED_URL=https://contrataciondelestado.es/sindicacion/sindicacion_643/licitacionesPerfilesContratanteCompleto3.atom

# CRM API (internal Docker network - 172.17.0.1 is Docker host from container)
CRM_API_URL=http://172.17.0.1:8000/api/oportunidades/spotter-internal

# Logging
LOG_DIR=/app/logs
